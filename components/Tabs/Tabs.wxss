
.tabs_title {
  display: flex;
  background-color: #fff;
}
.tabs_title .title_item {
  flex: 1;
  padding: 15rpx 0;
  text-align: center;
}
.active {
  color: var(--themeColor);
  border-bottom: 5rpx solid currentColor;
}